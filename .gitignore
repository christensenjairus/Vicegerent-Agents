# Python bytecode
__pycache__/
*.pyc
*.pyo

# Python venv
host/mcp/.venv/

# Machine plane copied from values.example.yaml (per-machine, never committed).
# Anchored to repo root so chart-internal charts/*/values.yaml stay tracked.
/values.yaml

# Git worktrees for branch work (see AGENTS.md worktree convention)
.worktrees/

# Host-side rclone serve s3 backing dir for Velero backups (never committed)
velero-backups/

# Hermes-Agent Project File and tmp folder
IDEA.md
.hermes/
