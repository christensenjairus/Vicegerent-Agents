# Generated by host/brew/generate.py; edit host/brew/templates/ghostunnel.rb.in instead.
class @CLASS_NAME@ < Formula
  desc "Simple SSL/TLS proxy with mutual authentication"
  homepage "https://ghostunnel.dev/"
  url "@URL@"
  version "@VERSION@"
  sha256 "@SHA256@" # pragma: allowlist secret
  license "Apache-2.0"
  keg_only :versioned_formula

  depends_on "go" => :build

  def install
    system "go", "build", *std_go_args(output: bin/"ghostunnel", ldflags: "-X main.version=#{version}")
    generate_completions_from_executable(bin/"ghostunnel", shell_parameter_format: "--completion-script-",
                                         shells: [:bash, :zsh])
  end

  test do
    assert_match version.to_s, shell_output("#{bin}/ghostunnel --version")
  end
end
