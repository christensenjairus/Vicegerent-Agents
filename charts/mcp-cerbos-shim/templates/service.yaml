apiVersion: v1
kind: Service
metadata:
  name: mcp-cerbos-shim
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: mcp-cerbos-shim
spec:
  selector:
    app.kubernetes.io/name: mcp-cerbos-shim
  ports:
    - name: grpc
      port: 4445
      targetPort: grpc
