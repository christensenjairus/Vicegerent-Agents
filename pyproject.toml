[project]
name = "vicegerent-repository-tools"
version = "0.0.0"
description = "Python dependencies for Vicegerent's host tooling and repository validation"
requires-python = ">=3.11"
dependencies = [
  "detect-secrets==1.5.0",
  "jsonschema==4.26.0",
  "pre-commit==4.6.1",
  "PyYAML==6.0.3",
  "rich==15.0.0",
  "textual==8.2.8",
  "uv==0.12.3",
]

[tool.uv]
package = false
